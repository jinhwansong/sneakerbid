import { auth } from './auth';
import { auctions } from './auctions';
import { orders } from './orders';
import { users } from './users';

export const api = {
  auth,
  auctions,
  orders,
  users,
};
